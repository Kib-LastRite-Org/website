# /create page passphrase protection
# Generate with: node scripts/gen-passphrase-hash.mjs <your-passphrase>
PUBLIC_CREATE_HASH=
PUBLIC_CREATE_SALT=

# Supabase — project: kib_lastrite_website (pxzyehlkxjmsogxdsusd)
# Get keys from: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://pxzyehlkxjmsogxdsusd.supabase.co
SUPABASE_ANON_KEY=                  # safe to commit, RLS restricts to published rows
SUPABASE_SERVICE_ROLE_KEY=          # SERVER-ONLY — never expose client-side

# API token for the /api/posts endpoint (POST + draft GET)
# Choose any strong random string — must match on server and client
CREATE_API_TOKEN=                   # server-side secret (no PUBLIC_ prefix)
PUBLIC_CREATE_API_TOKEN=            # same value, exposed to browser via Vite

GITHUB_PAT=
