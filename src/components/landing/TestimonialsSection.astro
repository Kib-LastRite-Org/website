---
---

<section
  class="testimonials"
  id="testimonials"
  aria-labelledby="test-heading"
>
  <div class="section-inner">
    <div class="section-header reveal">
      <h2 id="test-heading">Families who found their way through</h2>
      <p class="section-sub">
        Real families, real moments, real comfort.
      </p>
    </div>

    <div class="testimonials-grid">
      <blockquote class="testimonial-card glass-panel reveal" data-delay="1">
        <div class="stars" aria-label="5 out of 5 stars">★★★★★</div>
        <p>
          "It brought our whole family together in a way we didn't expect. We needed somewhere to grieve — together."
        </p>
        <footer>
          <cite>
            <strong>Sarah Jackson</strong>
            <span>Daughter, Ontario</span>
          </cite>
        </footer>
      </blockquote>

      <blockquote class="testimonial-card glass-panel reveal" data-delay="2">
        <div class="stars" aria-label="5 out of 5 stars">★★★★★</div>
        <p>
          "It didn't feel like an app — it felt like a place. Like somewhere to go when we just needed to be near him."
        </p>
        <footer>
          <cite>
            <strong>Michael Rossi</strong>
            <span>Son, California</span>
          </cite>
        </footer>
      </blockquote>

      <blockquote class="testimonial-card glass-panel reveal" data-delay="3">
        <div class="stars" aria-label="5 out of 5 stars">★★★★★</div>
        <p>
          "We didn't have to juggle WhatsApp, calls, and emails anymore. Everything was in one place — and we raised $25,000 in 48 hours."
        </p>
        <footer>
          <cite>
            <strong>Elena Kozlov</strong>
            <span>Granddaughter, New York</span>
          </cite>
        </footer>
      </blockquote>
    </div>
  </div>
</section>

<style>
  .testimonials {
    padding: 6rem 2rem;
    background: var(--color-background);
  }

  .section-inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-header h2 {
    margin-bottom: 1rem;
  }

  .section-sub {
    color: var(--color-on-surface-variant);
    font-size: 1.1rem;
    line-height: 1.6;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .testimonial-card {
    padding: 2rem;
    border-radius: var(--border-radius-card);
    border: 1px solid var(--color-outline-variant);
    display: flex;
    flex-direction: column;
    transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .testimonial-card:hover {
    transform: scale(1.02);
  }

  .stars {
    color: var(--color-secondary);
    font-size: 1.1rem;
    margin-bottom: 1rem;
    letter-spacing: 2px;
  }

  .testimonial-card p {
    color: var(--color-on-surface);
    font-size: 0.95rem;
    line-height: 1.7;
    margin-bottom: auto;
    font-style: italic;
  }

  .testimonial-card footer {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-outline-variant);
  }

  cite {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    font-style: normal;
  }

  cite strong {
    color: var(--color-on-surface);
    font-size: 0.95rem;
  }

  cite span {
    font-size: 0.8rem;
    color: var(--color-on-surface-variant);
  }

  @media (max-width: 720px) {
    .testimonials {
      padding: 4rem 1.25rem;
    }

    .testimonials-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
