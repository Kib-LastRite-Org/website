---
---

<header class="fixed top-0 left-0 right-0 z-50 h-16 flex items-center border-b border-white/5 bg-dark/80 backdrop-blur-md">
  <div class="max-w-6xl mx-auto w-full px-6 flex items-center justify-between">

    <!-- Logo -->
    <a href="/" class="flex items-center gap-2 text-mint font-bold text-xl tracking-tight hover:text-mint-light transition-colors">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true">
        <path d="M12 2C12 2 7 7 7 13a5 5 0 0 0 10 0C17 7 12 2 12 2Z" fill="currentColor" opacity="0.95"/>
        <rect x="11" y="17" width="2" height="5" rx="1" fill="currentColor" opacity="0.4"/>
      </svg>
      LastRite
    </a>

    <!-- Nav links (hidden on mobile) -->
    <nav class="hidden sm:flex items-center gap-8 text-sm text-gray-400">
      <a href="#features" class="hover:text-mint transition-colors duration-200">Features</a>
      <a href="#howitworks" class="hover:text-mint transition-colors duration-200">How It Works</a>
    </nav>

    <!-- CTA -->
    <a
      href="#download"
      class="px-4 py-2 rounded-btn border border-orange text-orange text-sm font-semibold hover:bg-orange hover:text-dark transition-all duration-200 active:scale-95"
    >
      Get Started
    </a>

  </div>
</header>
